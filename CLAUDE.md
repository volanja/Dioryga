# Dioryga

システム構築・運用プロジェクトのためのインフラ台帳。**「今どうなっているか」ではなく「どう変わってきたか」を第一級のデータとして扱う**点に、他の構成管理ツールとの違いがある。

このリポジトリは**実装**を扱う。要件定義・基本設計・設計上の相談は別リポジトリで行う。

## 設計書の場所

| | |
|---|---|
| 設計リポジトリ | `volanja/Dioryga_Design` |
| ローカル | `../Dioryga_Design/docs/design/basic-design.md`（兄弟ディレクトリ） |

設計書は**判断の経緯を含む議論記録**であり分量が大きい。日常の実装では、本リポジトリの `docs/spec/` を参照する。仕様の背景や「なぜそう決めたか」を知る必要があるときだけ設計書を開く。

**仕様に疑問や矛盾を見つけた場合、このリポジトリで勝手に決めない。**設計リポジトリの `docs/design/open-questions.md` に課題として追記し、設計書側で判断する。

| ファイル | 内容 |
|---|---|
| **`docs/schema/`** | **実装済みテーブルの正。**tblsが実DBから生成（ER図つき）。**手で編集しない** |
| `docs/spec/schema.md` | **未実装テーブルの暫定定義。**実装済みの節は、設計書のどの章に由来するかを示すため残している |
| `docs/spec/vocabularies.md` | enum値の語彙一覧 |
| `docs/spec/validation.md` | アプリケーション層で実装すべき検証規則 |

**このリポジトリには設計判断の経緯を書かない。**「なぜそう決めたか」は設計リポジトリの責務であり、両方に書くと必ず食い違う。`docs/spec/` に置く「なぜ」は、**実装者が意図的な設計を誤って"修正"しないために必要な最小限**に留める（例：「statusを持たないのは二重管理を避けるため」）。

## 技術スタック

| 項目 | 選定 |
|---|---|
| 言語 | Rust |
| Webフレームワーク | Axum（towerミドルウェアでRBAC等の横断的関心事を扱う） |
| DBアクセス | sea-orm |
| DB | PostgreSQL / SQLite 両対応（v1から） |
| フロントエンド | SSR（Askamaテンプレート）。**現時点でJavaScriptは使っていない。**htmxは画面単位の上乗せとして必要になった時点で導入する（設計リポジトリ `docs/design/frontend-htmx.md`） |
| 配布形態 | 単一バイナリ（rust-embedで静的アセット埋め込み） |
| i18n | rust-i18n（英語ベース＋日本語） |
| スキーマドキュメント | tbls（マイグレーション適用後の実DBから自動生成） |
| カバレッジ | cargo-llvm-cov |

**開発はMac、実行環境はWindows/Mac双方。**Windowsは開発に使わず、CI上でのビルドと起動スモークテストのみ行う。

**macOSのリリースビルド確認はCIでは毎回行わない**（macOSランナーはUbuntuの7.75倍の単価で、CI費用の6割を占めていた）。リリースタグ・週次・手動実行のみで走る。**日常的な確認は開発機で行う。**

```bash
cargo build --release --locked
```

**マイグレーションを足したらスキーマドキュメントを再生成すること。**再生成を忘れるとCIが落ちる。同じスクリプトで`tbls lint`も走り、**外部キー索引の張り忘れ**とテーブルコメントの欠落を検査する（両DBとも外部キー列に自動で索引を張らないため。設計書24.3）。Dockerが要る。

```bash
./scripts/schema-docs.sh
```

**依存を足したらライセンス表示を再生成すること。**再生成を忘れるとCIが落ちる。**`about.toml`の`accepted`に無いライセンスが入った場合は生成自体が失敗する**ため、GPL等が紛れ込んだことにも気付ける。生成物は`include_str!`でバイナリに埋め込まれ、`dioryga licenses`で出力される（単一バイナリで配る以上、これが全文を見る唯一の経路になる）。

```bash
./scripts/licenses.sh
```

## 破ってはいけない不変条件

実装中に**うっかり違反しやすい**ものを挙げる。いずれも設計全体の根幹であり、破ると設計の価値が失われる。

1. **事実は上書きしない。**履歴テーブル（`from_date`/`to_date`を持つもの）は、既存行を更新せず「旧行の`to_date`を閉じて新行を開く」。`UPDATE`で値を書き換えてはならない
2. **状態を保存せず計算で求める。**固定資産の簿価、年間コスト、消費電力の集計、空きIPアドレス、現在の構成——いずれも保存しない
3. **System Adminはプロジェクトデータに一切触れられない。**`is_system_admin=true` のユーザーによる `/projects/**` へのアクセスは、ロールの強弱と無関係に拒否する。これは独立したガードとして実装する（ロール判定に混ぜない）
4. **物理削除しない。**User（`disabled_at`）、Device（`merged_into_device_id`）、カタログのいずれも。`AUDIT_LOG`が参照しており、削除すると監査記録が壊れる
5. **参照されたカタログ行は編集不可。**スペックを定義するフィールドの更新前に、参照の有無をEXISTS判定する
6. **検証は原則ハードな禁止ではなく警告。**スロット本数の超過など、実機が仕様の想定外であることはありうる。誤って拒否すると事実を記録できなくなる
7. **DB機能に依存しない。**Row Level Security、トリガー、`jsonb`固有演算子、`LATERAL`などPostgreSQL専用機能は使わない。SQLiteと共通のSQLで書く（部分インデックスは両対応なので可）
8. **監査ログに機微情報を書かない。**`before_json`/`after_json`に認証情報の平文を含めない
9. **すべての画面は素のHTMLフォームだけで動く。**JavaScriptは上乗せであって前提にしない。`<form method="post">` で完結する経路を必ず残し、JSを切っても操作できる状態を保つ。htmxを導入する場合も画面単位の上乗せとし、htmx経由でしか動かない画面を作らない

## v1のスコープ

**含む**：System Admin領域、プロジェクトダッシュボード、メンバー管理、機器一覧・詳細・登録、ラック図、ネットワーク管理（IPアドレス一覧・インターフェース一覧・ポート接続）、SBOM取込、コスト・契約管理、マイルストーン、変更管理チケット、電力集計、倉庫、共有カタログ、個人設定、一括取込（カタログ＋Device＋配置）

**含まない（v2以降）**：物理構成図・論理構成図の描画、Ansible連携（APIトークン・外部からのWORK_ORDER遷移）、機器認証情報の管理、多要素認証・SSO、一括取込のうちPartInstance/ネットワーク/費用/ケーブル

データモデルはv2の範囲も含めて設計されている。**v1で作らないのは画面と機能であり、テーブル定義を削るわけではない**（後からのマイグレーションで既存データを作り直す事態を避けるため）。

## Git運用

**single issue / single branch。**issueごとにブランチを切り、PRを経てmainへマージする。設計リポジトリとは運用が異なる（あちらは設計段階のためmain直コミット）。
